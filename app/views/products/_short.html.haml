.span7.product{class: product.title_param, id: product_counter }
	.row
		.span3
			.product_thumbnail{id: product.title}=image_tag 'default.png'
		.span4
			.row
				%h3.span4=link_to product.title, product_path(product)
			.row
				-if product.users.include?(current_user)
					.span1=link_to "Add more", add_to_cart_user_product_path(current_user, product)
					.span1.quantity= "#{product.quantity_for(current_user)} in cart" if product.quantity_for(current_user) > 0
					.span2=link_to "Remove from cart", remove_from_cart_user_product_path(current_user, product)
				-else
					.span4=link_to "Add to cart", add_to_cart_user_product_path(current_user, product)
			.row.price
				.span1.base="$#{product.base_price}"
				%h3.span2.current="$#{product.price}"
			.row
				%h3.span4.discount="You save #{(100*(product.base_price - product.price)/product.base_price).round.to_i}%!"
			.row