$('.products').html("<%= j render partial: 'products/short', collection: @products, as: :product %>");
$(function() {

  $('.cart_btn').bind('ajax:beforeSend', function() {
    $(this).button("loading")
  });
  $('.cart_btn').bind('ajax:success', function() {
    $(this).button('reset');
  });

   $('li.category').removeClass("active");
  $("li.category.all").addClass("active");
});