.message
  Add product to category
.new_category
  %h3= @product.title
  = form_tag product_category_path(@product), html: {multipart: true}  do 
    .categories
      -@categories.each do |category|
        .category
          .check_form=check_box_tag "categories[#{category.id}]", category.name
          .label_form=label_tag "categories[#{category.id}]", category.name
    .new_category
      = label_tag :new_category, ''
      = text_field_tag :new_category, '', {placeholder: "Different...?"}
    =submit_tag 'Submit'