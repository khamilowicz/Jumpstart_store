  $(".products").html("<%= j render partial: 'products/short', collection: presentify(@products, current_user), as: :product %>");
  $('.paginate').html("<%= j paginate @products, remote: true %>");

  jQuery(document).ready(function($) {
    $('.cart_btn').bind('ajax:beforeSend', function() {
    $(this).button("loading")
  });
  $('.cart_btn').bind('ajax:success', function() {
    $(this).button('reset');
  });
    $('li.category').removeClass("active");
    $("li.category#<%= @category.id %>").addClass("active");
    $(".product_thumbnail, .product .title a").on('mouseenter', update_big_image);
  });